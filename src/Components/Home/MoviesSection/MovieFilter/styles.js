import styled from 'styled-components';

export const MovieCardsWrapper = styled.section`
  display: flex;
  flex-wrap: wrap;
  align-items: center;
  margin: 0 auto;
  max-width: 60rem;
`;

export default MovieCardsWrapper;
