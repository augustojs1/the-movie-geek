import React from 'react';
import axios from 'axios';
import { api, SEARCH_MOVIE } from '../services/api';

const Home = () => {
    React.useEffect(() => {
        
    }, [])

    return (
        <div>
            <h1>It's Live!</h1>
        </div>
    )
}

export default Home;
